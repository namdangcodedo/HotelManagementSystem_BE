# Copy this file to .env and update values before running docker-compose

# SQL Server SA password (must meet SQL Server complexity requirements)
SA_PASSWORD=YourStrong!Passw0rd

# Which image to run for the API. If you pushed the image to Docker Hub, set it here.
IMAGE=namcoder/hotel_management_be:tagname

# Host port to expose the API container on (maps to container port 80)
APP_PORT=8080

# Host port for SQL Server (if you want to access it from host)
MSSQL_HOST_PORT=1433

# ASPNETCORE environment
ASPNETCORE_ENVIRONMENT=Development

# JWT secret (optional override)
JWT_KEY=YourSuperSecretKeyAtLeast32CharsLong!!

# Cloudinary (optional override)
CLOUDINARY_CLOUDNAME=dmryi3rww
CLOUDINARY_APIKEY=964428731482642
CLOUDINARY_APISECRET=zXPJDunnpxWgvU5a-OOkTnqICQc

# Additional optional overrides
# FRONTEND URL etc. can also be exported as needed.

