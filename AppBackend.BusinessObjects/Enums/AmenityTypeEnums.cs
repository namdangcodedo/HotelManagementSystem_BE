namespace AppBackend.BusinessObjects.Enums
{
    public enum AmenityType
    {
        Common = 1,      // Tiện nghi cơ bản (miễn phí, có sẵn trong phòng)
        Additional = 2   // Tiện nghi bổ sung (có thể tính phí, yêu cầu thêm)
    }
}

